SELECT UNNEST ([1, 1, 1, 2])
UNION
SELECT UNNEST ([1, 1, 3]);

SELECT UNNEST ([1, 1, 1, 2])
UNION ALL
SELECT UNNEST ([1, 1, 3]);

SELECT UNNEST ([1, 1, 1, 2])
INTERSECT 
SELECT UNNEST ([1, 1, 3]);

SELECT UNNEST ([1, 1, 1, 2])
INTERSECT ALL
SELECT UNNEST ([1, 1, 3]);

SELECT UNNEST ([1, 1, 1, 2])
EXCEPT
SELECT UNNEST ([1, 1, 3]);

SELECT UNNEST ([1, 1, 1, 2])
EXCEPT ALL
SELECT UNNEST ([1, 1, 3]);

-- FINAL
CREATE OR REPLACE TEMP TABLE job_2023 AS 
SELECT * EXCLUDE (job_id, job_posted_date)
FROM job_postings_fact
WHERE EXTRACT(YEAR FROM job_posted_date) = 2023;

SELECT * FROM job_2023;

CREATE OR REPLACE TEMP TABLE job_2024 AS 
SELECT * EXCLUDE (job_id, job_posted_date)
FROM job_postings_fact
WHERE EXTRACT(YEAR FROM job_posted_date) = 2024;

SELECT * FROM job_2024;

-- WHICH UNIQUE JOBS POSTING APPEARED IN EITHER 2023 OR 2024?
SELECT 
    'jobs_2023' AS table_name,
    COUNT(*) 
FROM job_2023
UNION
SELECT  
    'jobs_2024' AS table_name,
    COUNT(*)
FROM job_2024;

SELECT * FROM job_2023
UNION
SELECT * FROM job_2024;

SELECT * FROM job_2023
INTERSECT
SELECT * FROM job_2024;

SELECT * FROM job_2023
UNION ALL
SELECT * FROM job_2024;

SELECT * FROM job_2023
EXCEPT
SELECT * FROM job_2024;

SELECT * FROM job_2023
EXCEPT ALL
SELECT * FROM job_2024;